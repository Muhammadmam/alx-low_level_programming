#include <stdio.h>
/**
 * main - Entry point
 * Descrition: 'print alphabet lowercase and uppercase'
 * Return: always 0
 */
int main(void)
{
	int n;

	for ( = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
